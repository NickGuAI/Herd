# Realtime

Server-owned realtime transcription proxy. The implementation currently lives
under `server/realtime`; this module directory keeps the capability manifest
classification explicit until the source is moved.
